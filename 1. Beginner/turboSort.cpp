#include<bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n,i;
    cin >> n;
    int arr[n];
    for(i=0;i<n;i++){
        arr[i] = 
    }
    return 0;
    }